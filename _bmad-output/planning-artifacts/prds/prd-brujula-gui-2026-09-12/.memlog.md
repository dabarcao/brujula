---
topic: Brujula Core backend/GUI decoupling PRD -- distilled from spec-brujula-core/SPEC.md and the architecture-brujula-gui-2026-09-11 spine
updated: 2026-09-12T16:34
---

- (decision) [ADOPTED] Stakes: internal tool (production app, solo/small-team operator, requirements already settled) -- target 5-8 pages, not a full launch PRD. Fast path chosen by user.
- (decision) [ADOPTED] Scope: this PRD covers CAP-1..CAP-5 from SPEC-brujula-core only (backend/GUI decoupling). The parallel 'Brujula Segura' visual redesign has its own finalized UX spines and is explicitly out of this PRD's scope.
- (assumption) [ASSUMPTION] Target User framing: the primary 'user' of this PRD's capabilities is the developer/operator (the product owner themselves, solo/small team) who builds and maintains Brujula -- not Brujula's own end-users directly, though end-user experience (esp. anonymity guarantees) must not regress. This is an infrastructure/internal-tooling PRD, so 'this is for me as the builder' framing applies per PRD Discipline guidance.
- (decision) [ADOPTED] Key User Journeys kept lightweight per the Scope Dial (internal tool, single operator role) -- two short journeys: UJ-1 the developer migrating the report-groups POC domain, UJ-2 an anonymous evaluator's experience staying unchanged through the migration (the invariant this whole initiative must not break).
- (decision) [ADOPTED] Feature/FR mapping: FR-1..FR-2 from CAP-1 (boundary enforcement, split into the lint rule and the server-only guard as two testable consequences of one FR), FR-3 from CAP-2 (dual exposure), FR-4 from CAP-3 (app-owned token + RLS backstop), FR-5 from CAP-4 (invitee continuity), FR-6+FR-7 from CAP-5 (split into feature-flag rollback and minimum test safety net as two FRs since they are independently testable requirements bundled under one capability in SPEC.md).
- (decision) [ADOPTED] Non-Goals section pulled verbatim in substance from SPEC.md's 4 non-goals (RLS-to-app-code rewrite, actual Postgres swap, CI/staging introduction, visual redesign scope).
- (decision) [ADOPTED] MVP Scope: in-scope = layering scaffolding (db/managers/api skeleton, ESLint boundary rule, app token issuance) + the report-groups domain fully migrated (the POC, per architecture spine AD-8). Out of scope for this MVP = the remaining 5 domains' migration (sequenced later per AD-8), Supabase removal (non-goal), CI introduction (open question).
- (decision) [ADOPTED] Success Metrics: SM-1 (primary, long-term) = SPEC.md's full success signal (all 6 domains migrated, flags removed, 0 lint violations repo-wide). SM-2 (secondary, MVP-scoped) = report-groups POC works end to end against seeded demo data with its flag on. SM-C1 (counter-metric) = migration velocity must not be optimized at the expense of the per-domain manual-QA/test-safety-net step (constraint: zero existing test coverage, no CI/staging) -- do not skip verification to hit a schedule.
- (decision) [ADOPTED] Open Questions carried forward verbatim from SPEC.md: (1) whether/how to introduce CI, decide before the POC is considered done; (2) exact token issuance/refresh/CSRF mechanics, scoped to the scaffolding epic.
- (assumption) [ASSUMPTION] Vision (Sec 1) and Document Purpose (Sec 0) paragraphs are elaborated prose distilled from SPEC.md's terse Why paragraph and the architecture spine's context -- not verbatim quotes, since SPEC.md's Why is a single dense paragraph and the PRD template expects 2-3 standalone paragraphs.
- (event) Reviewer/reconciliation pass: rubric walker (2 high, 2 medium, 1 low) + SPEC.md reconciliation (3 minor rationale gaps) + architecture-spine reconciliation (0 inconsistencies, 3 product-relevant gaps). All findings applied: FR-4/section 2.2 numbering gaps closed (FR-5->4, FR-6->5, FR-7->6, UJ section renumbered 2.3->2.2); CAP-1..CAP-5 traceability tags added to every FR; SM-1/SM-2 relabeled Primary(MVP gate)/North Star to match MVP scope; 'one full production cycle' defined concretely in FR-5; dual-auth maintenance cost named in 4.3; RLS/RPC/migration-discipline rationale (9 migrations, 66 migrations, 2 prior RLS-recursion bugs) restored to Glossary/4.5; AD-9 Vitest Server-Component-testing scope caveat added to FR-6; AD-6 middleware/session-refresh discrepancy check added to MVP in-scope; AD-8 UI-redesign sequencing note added to the Non-Goals UX touchpoint; Assumptions Index roundtrip gap fixed (distillation-method disclosure separated from true assumptions).
- (event) PRD finalized
- (decision) [AMENDMENT, post-finalization, same day] FR-6 amended to make the characterization-testing discipline explicit (baseline captured against current behavior before refactoring, re-verified at each migration stage), mirroring the architecture spine's AD-9 amendment. Surfaced during bmad-sprint-planning's readiness gate.
