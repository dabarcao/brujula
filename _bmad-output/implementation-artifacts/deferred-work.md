- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-characterization-tests-report-groups-baseline.md`
  summary: Add characterization coverage for authorization boundaries (non-member/non-invitee calling respondToReportGroup/closeReportGroup), repeat-action idempotency (double-respond, close-already-closed), and a mixed eligible+ineligible member list in one create call.
  evidence: Real gaps for a refactor-safety baseline; underlying behavior is already enforced by the existing RPCs (respond_to_report_group requires an invited-member row, close_report_group requires accepted-member status), but expanding the approved 4-scenario I/O matrix now is scope growth beyond Checkpoint-1 approval — better as a fast follow-up.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-characterization-tests-report-groups-baseline.md`
  summary: Extract the duplicated login/restGet/RPC-call helpers in scripts/seed-report-group.mjs and tests/characterization/report-groups.test.ts into one shared module.
  evidence: The spec explicitly wants seed-report-group.mjs's shape reused by Epic 3's later domains, but the smallest fix (extract a shared module) is more than a direct correction — better timed for Epic 3's first actual reuse than speculative extraction now.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-characterization-tests-report-groups-baseline.md`
  summary: Add afterAll teardown for seeded companies/report groups in the characterization test suite.
  evidence: Local Supabase instance accumulates orphaned test data across repeated runs. Low urgency — unique timestamped company names already avoid collisions, and `supabase db reset` is an existing escape hatch. Fix is more than a direct correction.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-characterization-tests-report-groups-baseline.md`
  summary: Replace beforeAll's regex-parsing of seed-demo-company.mjs's console output with a structured/programmatic result.
  evidence: Brittle coupling to an established script's exact log wording — a future wording change would silently break this test. Proper fix (structured output mode on seed-demo-company.mjs) touches a file outside this story's stated Code Map scope.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-enforce-the-import-boundary.md`
  summary: Add `import "server-only";` to `src/lib/supabase/client.ts` and `src/lib/supabase/server.ts` as well, not just the new `db`/`managers` files.
  evidence: Real gap — these wrapper files are exactly the kind of module that must never end up in a client bundle, and today they have no defense-in-depth guard at all. Not fixed in Story 1.3 because its own frozen "Never" list scoped the story's footprint to the four named Story 1.2 files plus ESLint config/package.json — touching src/lib/supabase/* was explicitly out of bounds.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-enforce-the-import-boundary.md`
  summary: Add a lasting regression check (an ESLint Node-API fixture test, or a CI step) proving the no-restricted-imports boundary rule still fires, instead of relying only on the one-time scratch-file proof this story used.
  evidence: Real, ongoing risk — over Epic 3's 30 remaining migration stories, a careless edit to eslint.config.mjs (dropped block, reordered exception list, typo) would silently regress the boundary with nothing in `npm run lint`/`npm run test` catching it, since no CI exists anywhere in this repo today. Not fixed now because the frozen spec explicitly scoped verification as "one-time, not a lasting artifact," and building persisted regression-test infrastructure is bigger than a patch — this is the same open decision the architecture spine already flagged ("whether/how to introduce CI... revisit before the report-groups POC is considered done").

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-app-token-issuance-and-validation.md`
  summary: Add an `.env.example` (or equivalent onboarding/deployment doc) documenting every required env var, including the new `APP_TOKEN_SECRET`, for a real (staging/production) deployment.
  evidence: Pre-existing, repo-wide gap, not introduced by this story — no `.env.example` exists anywhere in the repo, and the project's two original required env vars (`NEXT_PUBLIC_SUPABASE_URL`/`_ANON_KEY`, present since the initial commit) have never been documented for deployment either. Worth fixing before this app-token mechanism actually gets deployed anywhere real, but it's a repo-wide onboarding gap bigger than this one story's patch budget.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-report-groups-route-handler-and-client-fetch-wrapper.md`
  summary: Have `reportGroupsManager.closeGroup` return a richer result distinguishing "closed, interpretation saved" from "closed, but the interpretation save failed," so the Route Handler can return 200 (with a warning) instead of 422 when the group itself did close successfully.
  evidence: Real, user-visible bug — closeGroup can throw after closeReportGroup already succeeded (a saveReportGroupInterpretation failure, per Story 1.2's own reviewed fix that intentionally lets this propagate), and the Route Handler's uniform error mapping then tells the client the request failed (422) even though the state-changing operation actually succeeded. Not fixed in Story 1.5 because the real fix changes closeGroup's return contract, which is Story 1.2's already-committed, frozen spec — out of this story's own file-scoped patch budget.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-report-groups-server-actions-become-thin-delegates.md`
  summary: Extract the `try { await managerFn(...) } catch (error) { redirect(...) }` shape duplicated across all three Server Actions in `src/app/actions/reportGroups.ts` into a shared helper, once a second domain's Server Actions need the identical pattern.
  evidence: This is explicitly the template later Epic 1/3 domain stories (cycles, feedback, etc.) are meant to copy — leaving it un-factored means every future domain hand-copies the same boilerplate instead of reusing it. Matches this initiative's established extract-on-first-real-reuse pattern (Story 1.1's login/restGet helpers, Story 1.5's route-group-scoped `_shared.ts`) — premature to extract with only one domain built so far.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-report-groups-server-actions-become-thin-delegates.md`
  summary: Update `tests/characterization/report-groups.test.ts`'s header comment, which still describes `src/lib/aiInterpretation.ts`'s `generateReportGroupInterpretation` as what's exercised "for real" — no longer accurate now that `closeReportGroup` delegates through `reportGroupsManager.closeGroup` to `aiInterpretationManager.ts` (Story 1.2's separately-maintained copy).
  evidence: Real staleness that could send a future debugger to the wrong file. Not fixed in Story 1.6 because its own frozen "Never" list explicitly forbids modifying that test file, even for a comment-only change, to keep it an untouched equivalence baseline — flagged for whenever the file is next legitimately touched (likely Story 1.8's final baseline pass).

- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-core-interactive-primitives.md`
  summary: Adopt a proper className-merge utility (e.g. `tailwind-merge`) in `ButtonPrimary`/`ButtonSecondary`/`RevealButton` so caller-supplied `className` reliably overrides the components' own conflicting utilities (e.g. `px-6 py-3`), instead of relying on Tailwind's generated-CSS order.
  evidence: Real gap — plain string concatenation doesn't guarantee override precedence in Tailwind, since class-attribute order doesn't determine which utility wins. Not fixed now because the correct fix is a new dependency, bigger than this story's footprint, and no current consumer is exposed to it yet (these components aren't wired into any page until Stories 2.6/2.7/Epic 4).

- source_spec: `_bmad-output/implementation-artifacts/spec-2-4-feedback-and-status-components.md`
  summary: Fix DESIGN.md's own frontmatter, which still lists `bookmark-chip.radius: '{rounded.sm}'`, contradicting its own prose (the "## Components" and "## Shapes" sections both independently call bookmark-chip a pill, i.e. `{rounded.full}`).
  evidence: Real, verified self-contradiction in a frozen, `status: final` planning artifact from an earlier BMad phase (bmad-ux) — confirmed by reading both prose sections directly. `BookmarkChip.tsx` correctly implements `rounded-full` per the weight of evidence (two independent prose mentions vs. one isolated, likely copy-pasted frontmatter line), but the source document itself stays wrong for the next reader who trusts the frontmatter. Not fixed by this build-story since DESIGN.md belongs to a different, already-finalized planning phase, not this story's own scope.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-5-scale-slider-restyle.md`
  summary: If the `ScaleSlider`'s unfilled track segment (still browser UA-default gray, not `--color-line`) is ever visually flagged as unacceptable, add cross-browser `::-webkit-slider-runnable-track`/`::-moz-range-track` pseudo-element CSS to fully match DESIGN.md's `scale-slider.track: '{colors.line}'` spec.
  evidence: Real gap between DESIGN.md's literal token spec and `accent-indigo`'s actual browser behavior (which only colors the thumb and filled portion, not the unfilled track). This story's own frozen Approach already explicitly chose `accent-indigo` as "the closest native mechanism," knowingly accepting the approximation rather than adding cross-browser pseudo-element CSS, which exceeds a restyle-only story's minimal-footprint scope.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-6-report-groups-screens-redesign.md`
  summary: Restyle `src/components/EvaluatorPicker.tsx`'s own submit button (still hand-rolled `bg-black`/`hover:bg-gray-800`) to use `ButtonPrimary`, so the "Crear grupo" CTA on `/dashboard/groups/nuevo` stops being the one un-tokenized interactive element on an otherwise fully-restyled page.
  evidence: Real gap traced to a wrong assumption in this story's own frozen spec (that EvaluatorPicker's `primary` prop already produced tokenized styling — it doesn't). Not fixed here because `EvaluatorPicker` is used in multiple contexts beyond report-groups and deserves its own careful, scoped restyle pass, not a side-effect of this one story; also outside this story's own explicit "Never touch EvaluatorPicker.tsx" boundary.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-6-report-groups-screens-redesign.md`
  summary: Revisit DESIGN.md's `focus-ring` component spec — `colorOnFilled: '{colors.paper-deep}'` has very low contrast against the page background (`paper`) in both light and dark themes, a real WCAG focus-visibility problem now that `ButtonPrimary` is in live use for the first time.
  evidence: DESIGN.md's own reasoning for this color choice ("so the ring is never drawn against a fill it can't be seen on") assumes the ring overlaps the button's fill, but its own stated mechanism ("drawn outside the element bounds") means the ring actually sits against the page background, not the fill — an internal tension in the frozen design system's own spec, not an implementation error. Computed token values confirm the resulting contrast genuinely fails a visibility bar. Not fixed here since overriding DESIGN.md's explicit color assignment is a design-system-level decision outside a screens-redesign implementation story's authority.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-7-invitacion-accept-decline-redesign.md`
  summary: `src/app/dashboard/page.tsx`'s "Tareas pendientes" list has a second, unstyled accept/decline entry point calling the same `respondToReportGroup` action (plain gray Tailwind classes, no design-system buttons) that never gets the checkmark acknowledgment/asymmetric treatment this story added at `/dashboard/groups/[id]`.
  evidence: Real UX inconsistency — the same action can be triggered from two different pages, only one of which now shows the acknowledgment. Not fixed here: this story's own frozen Intent explicitly scoped investigation and implementation to `/dashboard/groups/[id]` only, matching epics.md's Story 2.7 AC (one page), and `/dashboard/page.tsx` itself hasn't been restyled by any Epic 2 story yet — likely out of scope until a dedicated dashboard-restyle pass.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-2-db-access-manager-scaffolding-admin-members-auth.md`
  summary: `adminManager.checkIsPlatformAdmin`, `membersManager.listMembers`, and `membersManager.claimPendingInvitations` have no direct test coverage in `tests/characterization/admin-members-auth-manager.test.ts` — all three lens reviews (Blind Hunter, Edge Case Hunter, Verification Gap) independently confirmed this.
  evidence: Low risk — each is a one-line pass-through delegate to an already-tested `db/*` function (mirroring Story 1.2's proven `reportGroups.ts`/`reportGroupsManager.ts` shape), and `tsc`/`lint`/the passing suite give no signal on whether they behave correctly once wired to a real caller. Not added now because the frozen I/O matrix's 6 scenarios didn't call for them; real exercise happens naturally when Story 3.4 wires these into the Server Actions and a manual/automated pass exercises them end-to-end.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-3-admin-members-route-handlers-client-fetch-integration.md`
  summary: Consolidate `src/app/api/_shared.ts` (this story's new top-level `jsonError`/`requireAuthorizedRequest` pair) with `src/app/api/report-groups/_shared.ts` (Story 1.5's route-group-scoped original, left untouched), which are now byte-for-byte duplicates — including the `[report-groups]`-tagged internal `console.warn`/`console.error` messages in the new top-level copy, which are misleading when logged from an admin/members/auth route. A second, smaller instance of the same duplication pattern: the anti-CSRF header check (`if (!request.headers.get(CSRF_HEADER)) return jsonError(403, ...)`) is also duplicated verbatim inline in both `src/app/api/auth/signin/route.ts` and `src/app/api/auth/signout/route.ts`, since it deliberately isn't part of `_shared.ts`'s two exported functions.
  evidence: Deliberate, spec-frozen duplication (this story's own Approach explicitly chose "one short-lived duplicated file" over touching Epic 1's already-shipped, frozen `report-groups/_shared.ts`, to avoid any regression risk to that domain). Not consolidated now because the frozen spec scoped this story to a verbatim copy; a real fix (single shared module, every `src/app/api/**/route.ts` importing from it, `report-groups/_shared.ts` deleted, plus a shared CSRF-check helper) touches Epic 1's frozen code and is bigger than this story's own patch budget — worth doing on Epic 3's next domain that would otherwise need a third copy.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-6-admin-members-new-path-verification.md`
  summary: This story's `login()`/`signUpOrSignIn()` test helpers (copied from Story 3.1's baseline, same as every other test file in this domain) lack guards for a couple of edge cases: a login response missing `access_token`, and signup failing for a reason other than already-registered.
  evidence: Real but pre-existing, inherited boilerplate — not introduced by this story. Same underlying concern as Story 1.1's already-logged deferred-work.md entry about extracting the duplicated login/restGet/RPC-call helpers into one shared module; worth fixing there, not separately in each domain's copy.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-5-admin-members-feature-flag-rollback-safety.md`
  summary: Only 4 of the admin/members/auth domain's 9 flag-gated call sites have toggle-routing test coverage (`createOrganizationAsAdmin`, `inviteMember`, `signIn`, and the dashboard's `acceptInvite` branch) — `updateOrganizationName`, `createDepartment`, `signOut`, and the dashboard's `checkIsPlatformAdmin`/`claimPendingInvitations` branches have none.
  evidence: Real gap, but explicitly excluded by this story's own frozen Intent/Boundaries, which deliberately scoped coverage to "a representative sample... at least one call site each" across the 4 touched files rather than exhaustive coverage (full old-vs-new equivalence testing is Story 3.6's separate job). Worth broadening if a future domain's equivalent story wants fuller toggle-mechanism coverage, or if Story 3.6 finds it needs this.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-7-characterization-tests-cycles-baseline.md`
  summary: The "add-only" branch of `update_cycle_request_evaluators`/`update_individual_cycle_request_evaluators` (triggered once a request already has responses) is uncharacterized -- only the "full replace" (zero-response) branch is tested. `get_request_competency_comparison`'s reveal-threshold behavior (silently empty results when response count is below the minimum) is likewise uncharacterized.
  evidence: Both real, verified gaps, but each requires seeding a request into a specific partial-response state -- materially more fixture complexity than this story's other patches. Worth closing in Story 3.12's deeper new-path verification pass, or whenever this domain's tests are next substantially touched.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-7-characterization-tests-cycles-baseline.md`
  summary: Several test-helper functions in `tests/characterization/cycles.test.ts` (`login`, `restGet`, `errorFromRedirect`, the per-evaluator category-array builder, `resolve()`) lack defensive guards for malformed-response/fixture-mismatch edge cases.
  evidence: Real but low-impact -- would surface as confusing errors only if the local Supabase instance or seed script's output shape changed unexpectedly. Matches the same class of test-infrastructure hardening already deferred in Stories 3.5/3.6's own reviews.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-7-characterization-tests-cycles-baseline.md`
  summary: `finalizeCycleRequest`'s AI-interpretation "text" branch (vs. "null") is never actually exercised locally, since `ANTHROPIC_API_KEY` is unset in `.env.test.local` and `generateAiInterpretation` short-circuits to `null` before reaching `save_ai_interpretation`.
  evidence: Real, but the exact same weak-assertion shape already accepted in three already-merged Epic 1 files (`report-groups.test.ts`, `report-groups-manager.test.ts`, `report-groups-route.test.ts`) for the identical non-determinism problem -- a repo-wide precedent question, not unique to this story.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-8-db-access-manager-scaffolding-cycles.md`
  summary: A couple of test-helper functions in `tests/characterization/cycles-manager.test.ts` lack defensive guards for fixture-mismatch edge cases (no matching supervisor row, no cycle request found for an employee).
  evidence: Real but low-impact -- matches the identical class of test-infrastructure hardening already deferred in Story 3.7's own review, for the same reasons (would only surface as confusing errors if the seed script's output shape changed unexpectedly).

- source_spec: `_bmad-output/implementation-artifacts/spec-3-9-cycles-route-handlers-client-fetch-integration.md`
  summary: All 8 new cycles Route Handlers re-declare nearly identical `invalidBody`/`invalidId` closures and repeated array-validation blocks, the same duplication pattern already flagged once for `src/app/api/_shared.ts` in Story 3.3.
  evidence: Real, matches the established "extract-on-first-real-reuse" pattern already deferred multiple times this session. This is arguably the second real reuse now (admin/members + cycles both have it) -- worth extracting on the next domain that needs it, or in a dedicated refactor story, since touching it now would mean editing already-shipped admin/members route files.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-9-cycles-route-handlers-client-fetch-integration.md`
  summary: No cycles route validates that `evaluatorCategories` array elements are actually members of the `CycleParticipantCategory` union -- only that each element is a `string`.
  evidence: Real but matches the accepted, repo-wide validation-depth convention (admin/members routes have the identical limitation, never patched). Enforcing enum membership consistently across every category-accepting route is a moderate scope expansion, not a trivial fix.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-10-cycles-server-actions-thin-delegates.md`
  summary: `src/server/db/cycles.ts`'s and `src/server/managers/cyclesManager.ts`'s header comments still describe `src/app/actions/cycles.ts` as "unmodified by this story... until Story 3.10 refactors it" -- now stale, since Story 3.10 did exactly that.
  evidence: Real staleness that could mislead a future reader. Not fixed by Story 3.10 itself because its own frozen Never list explicitly scopes `db/cycles.ts`/`cyclesManager.ts` as read-only. Same treatment as the identical class of finding already deferred in Story 1.6 -- flagged for whenever these files are next legitimately touched.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-11-cycles-feature-flag-rollback-safety.md`
  summary: `docs/feature-flags.md`'s new `USE_NEW_API_CYCLES` section hands off equivalence verification to Story 3.12 via a cross-reference to "AD-3's documented cycle-specific list" (3 RPCs), but `db/cycles.ts` already wraps 8 -- the doc doesn't flag that this staleness could trip up Story 3.12's own gate.
  evidence: Speculative concern about a future story's scope, not a defect in this diff's own deliverables. Story 3.12 will be investigated fresh (same discipline as every prior story) when it's written, so this is unlikely to actually cause confusion, but noted here in case it does.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-12-cycles-new-path-verification.md`
  summary: `tests/integration/cycles-route.test.ts` (Story 3.9) has non-Supervisor/non-owner rejection coverage for only 1 of 7 cycles routes -- and that one (`GET /api/cycles/[cycleId]/status`) is itself a read, not a mutation. All 6 mutating routes (create/organize/close/update, both variants) are tested only for Supervisor-success or business-rule RPC rejections, never an identity/access rejection.
  evidence: Real gap, analogous to the one Story 3.6 closed for admin/members. Not fixed by Story 3.12 because epics.md's own AC for that story is a static RPC-ownership check, not an access-boundary requirement (unlike Story 3.6's AC2) -- worth closing in a future revisit of the route suite, or whenever cycles' access-control model is next touched.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-4-admin-members-server-actions-thin-delegates.md`
  summary: The shared flag check (`process.env.USE_NEW_API_ADMIN_MEMBERS === "true"`) is re-typed as a literal string at 8 separate call sites across `src/app/actions/{admin,members,auth}.ts` and `src/app/dashboard/page.tsx`, with no shared constant or helper.
  evidence: Real but currently non-manifesting — a future typo at any one of the 8 sites would silently and permanently pin that one branch to the old path with nothing to catch it structurally. Not fixed now because this is the first flag this initiative has introduced (no established shared-flag-helper location exists yet to extract to); worth revisiting once a second domain's flag story (e.g. Story 3.11 for cycles) shows whether the pattern generalizes into something worth a real shared helper.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-3-admin-members-route-handlers-client-fetch-integration.md`
  summary: The uniform `Error` → 422 `validation_error` mapping (inherited verbatim from Story 1.5's Route Handler pattern) doesn't distinguish genuine backend/infra failures (e.g. a dropped DB connection during `is_platform_admin()`) from legitimate business-rule rejections (e.g. wrong password, non-admin caller) — both look identical to callers. `POST /api/auth/signin` additionally has no rate-limiting/throttling and is a stable, publicly discoverable URL, unlike the Server Action it will eventually replace.
  evidence: Both real but pre-existing/systemic, not introduced by this story: the error-mapping pattern was already shipped and reviewed in Story 1.5 and this story's frozen spec explicitly required mirroring it exactly; no route or Server Action anywhere in this repo has rate-limiting today. Worth a dedicated cross-cutting hardening story once more security-sensitive domains (admin, auth) are exposed this way — not a one-route patch.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-14-db-access-manager-scaffolding-feedback.md`
  summary: No mechanical (lint/test) enforcement that a domain manager (e.g. `feedbackManager`) only imports its own `db/*` file and never Supabase directly or another domain's manager — the boundary is asserted only in header-comment prose.
  evidence: Pre-existing architectural gap across every domain's manager since Story 1.2/3.2/3.8 introduced the pattern, not introduced by this story. Worth a dedicated ESLint rule once enough managers exist to make the rule's shape obvious (mirrors this initiative's existing `src/app/**` → `@/lib/supabase/*` import-boundary rule from Epic 1).

- source_spec: `_bmad-output/implementation-artifacts/spec-3-14-db-access-manager-scaffolding-feedback.md`
  summary: `tests/characterization/feedback-manager.test.ts` duplicates several helpers/types verbatim from `tests/characterization/feedback.test.ts` (`respondAsEvaluator`, `login`/`signUp`/`restGet`/`callRpc`, `ResponderContext`/`ResponderQuestion`) instead of sharing a module.
  evidence: Mirrors the identical, already-accepted `cycles.test.ts`/`cycles-manager.test.ts` duplication from Story 3.8 — this initiative's established extract-on-reuse pattern says wait for the domain that actually needs a third copy before extracting, not speculatively extract on the second.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-14-db-access-manager-scaffolding-feedback.md`
  summary: `db/feedback.ts`'s `createAdHocFeedbackRequest`/`createAdHocFeedbackRequestForIndividual` don't guard against `data` being null/undefined when `error` is null before returning it as the new request's id.
  evidence: Mirrors the identical, already-accepted pattern in `db/cycles.ts`'s `createFeedbackCycle` (Story 3.8) — not introduced by this story, and every Postgres RPC these wrap always returns a non-null uuid on success (its final statement is `return new_request_id`, never a bare `return`), so the gap is real only under an infra-level anomaly no test in this repo currently demonstrates.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-14-db-access-manager-scaffolding-feedback.md`
  summary: `tests/characterization/feedback-manager.test.ts`'s fixture helpers have no explicit throw if a seeded email has no matching member id (`idByEmail[email]` silently `undefined`) or if `ctx.competencies` is empty for a competency-type question (`respondAsEvaluator` submits `competency_code: undefined`).
  evidence: Same class of test-helper hardening gap already logged for Stories 3.7/3.8/3.12/3.13 — these guard omissions would only matter if the seed script or auth itself were already broken, in which case the resulting failure, while less clearly worded, would still fail loudly rather than silently pass.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-15-feedback-route-handlers-client-fetch-integration.md`
  summary: All 7 new `src/app/api/feedback-requests/**` route files re-declare identical `invalidId`/`invalidBody` closures and inline array-shape validation instead of sharing them — now a third occurrence of a pattern already flagged for Story 3.3's `_shared.ts` and Story 3.9's 8 cycles routes.
  evidence: This initiative's established extract-on-reuse pattern keeps not triggering because each domain's own route count alone doesn't force the issue — worth extracting once a cross-domain route-validation helper module is actually needed, not preemptively.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-15-feedback-route-handlers-client-fetch-integration.md`
  summary: `src/app/api/_shared.ts`'s `requireAuthorizedRequest()`/`jsonError()` still hardcode `[report-groups]`-tagged `console.warn`/`console.error` messages even though the file was generalized to cross-domain use in Epic 3 — every route across admin/members/cycles/feedback now inherits the mislabeling on every 401/403/500 rejection.
  evidence: Pre-existing since the file's Epic 3 generalization (Story 3.2), not introduced by any single domain's route story — a one-line label fix (`[report-groups]` → something domain-agnostic like `[api]`), trivial but touches a file frozen by every domain route story's own Never boundary, so no single domain story can fix it without renegotiating scope.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-15-feedback-route-handlers-client-fetch-integration.md`
  summary: The non-`Error`-throw → `500 internal_error` catch branch in all 7 new feedback routes is untested, and no rate-limiting/throttling exists on the two newly-public create endpoints (`POST /api/feedback-requests`, `POST /api/feedback-requests/individual`).
  evidence: Both are pre-existing, systemic gaps already present and accepted in every prior domain's routes (Story 1.5/3.3/3.9) — `db/feedback.ts` wraps every RPC failure as `throw new Error(...)`, leaving no realistic path to the untested branch; no route or Server Action anywhere in this repo has rate-limiting today (already flagged in Story 3.3's own deferred-work entry for `POST /api/auth/signin`).

- source_spec: `_bmad-output/implementation-artifacts/spec-3-15-feedback-route-handlers-client-fetch-integration.md`
  summary: All 4 `[id]`-path-param feedback routes' `await params` is unguarded against a theoretical promise rejection, outside the try/catch that guards the manager call.
  evidence: Confirmed to be an exact mirror of the pre-existing Story 3.9 precedent (`cycles/requests/[requestId]/close/route.ts`, unchanged) — not introduced by this diff, and Next.js's `params` promise does not realistically reject in practice.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-17-feedback-feature-flag-rollback-safety.md`
  summary: `docs/feature-flags.md`'s Manual QA checklists (both `USE_NEW_API_CYCLES` and `USE_NEW_API_FEEDBACK` sections) name "confirm access is still correctly denied at every call site" as a manual-only step, with zero automated coverage of access-denial behavior around either flag.
  evidence: Pre-existing, systemic pattern established by Story 3.11 for cycles and carried forward identically here — not introduced by this story. Worth a dedicated automated access-boundary regression once enough domains' flags exist to make a shared test shape obvious.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-18-feedback-new-path-verification.md`
  summary: `get_request_competency_narrative`/`get_my_pending_invitations` are exercised only via raw RPC calls, never through `feedbackManager`'s equivalent functions or their already-shipped Story 3.15 Route Handlers (`GET /api/feedback-requests/[id]`, `GET /api/feedback-requests/pending-invitations`).
  evidence: Neither RPC is flag-gated anywhere in production code today — the dashboard pages that call them call the RPC directly, unrefactored (likely Epic 4's job) — so there is no actual gated "new path" caller to verify equivalence against yet. Matches Story 3.12's own identical, already-accepted precedent for the symmetric `get_cycle_status`/`get_colleagues_with_closed_cycle` situation. `tests/characterization/feedback-manager.test.ts` (Story 3.14) already covers the manager functions' own correctness directly.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-18-feedback-new-path-verification.md`
  summary: `tests/integration/feedback-new-path-verification.test.ts` seeds exactly one 8-employee demo company and allocates all 8 as one-time-use requester fixtures, with no comment flagging that the budget is fully spent.
  evidence: Minor maintainability note — a future contributor adding one more Section-1 scenario to this file would need a fresh employee slot (a second seed call, or reusing a non-requester employee) with no signal that an available spare doesn't exist. Low cost to note inline whenever the file is next touched; not a current defect.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-19-characterization-tests-responder-invitation-baseline.md`
  summary: `submit_feedback_response`'s two remaining unexercised error paths (foreign question ids not belonging to the request's template; competency-count/existence validation — too many competencies selected, or a competency code that doesn't exist) have no characterization coverage.
  evidence: Generic input-shape guards, less specific to this domain's own distinguishing trait (the auth-model split) than the identity-mismatch guard this story's own review already added coverage for. Worth closing once a future story in this domain touches this RPC's validation surface directly.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-19-characterization-tests-responder-invitation-baseline.md`
  summary: `tests/characterization/responder-invitation.test.ts`'s `buildAnswersFormData` helper's `"competency"` branch is never exercised in this file — no fixture here pairs a competency-bearing template with `submitFeedbackResponse` as the primary subject under test.
  evidence: The RPC-level competency-answer submission path is already exercised elsewhere in the suite (`feedback.test.ts`, `feedback-manager.test.ts`, `feedback-new-path-verification.test.ts` all call `submit_feedback_response` directly with competency answers as fixture-building plumbing) — just never through this specific Server Action. Low urgency; would need a dedicated `competencias`-subtype ad-hoc request fixture to close.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-19-characterization-tests-responder-invitation-baseline.md`
  summary: No teardown/cleanup for the demo companies, employees, and individual accounts each characterization test run creates in the local Supabase instance.
  evidence: Pre-existing, already logged for Story 1.1 and repeatedly since — not introduced by this story. `Date.now()`-suffixed uniqueness avoids cross-run collisions; `supabase db reset` remains the existing escape hatch.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-19-characterization-tests-responder-invitation-baseline.md`
  summary: `scripts/seed-demo-company.mjs`'s hard-required `platform_admins` bootstrap row (for a specific fixed email, per the script's own header comment) as a precondition for any suite using it isn't documented in this story's own artifacts.
  evidence: Pre-existing requirement of the shared seed script used by every ad-hoc-domain characterization suite since Story 1.1, not introduced by this story.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-20-db-access-manager-scaffolding-responder-invitation.md`
  summary: No lint rule or test enforces that `responderManager.ts` never calls `requireApiToken()` — the domain's one explicitly-called-out structural deviation (AD-7/FR4) is guarded only by a comment.
  evidence: Matches the identical, already-logged systemic gap for manager-layer import boundaries generally (Story 3.14's deferred-work entry) — worth a dedicated ESLint rule once enough managers exist to make the rule's shape obvious.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-20-db-access-manager-scaffolding-responder-invitation.md`
  summary: The self-evaluation (`is_self: true`) branch is never exercised through `responderManager` in `tests/characterization/responder-invitation-manager.test.ts`; nor are `submit_feedback_response`'s missing-required-answers and foreign/invalid-competency rejection paths.
  evidence: All three are already covered at the RPC level by Story 3.19's own baseline suite, and `responderManager` is a pure pass-through with no self/peer-specific or per-guard transformation logic — lower priority than the already-used branch and invalid-token case this story's own review already added coverage for.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-21-responder-invitation-route-handlers-client-fetch-integration.md`
  summary: The no-CSRF-header security rationale for `/api/responder/[token]/submit` only addresses forged-request construction, not token-confidentiality leakage — `feedback_invitations` has no `expires_at`, so a leaked token (via Referer header, browser history, access logs, or an evaluator forwarding their own link) stays valid indefinitely until first use.
  evidence: Real residual risk, but the mitigation (e.g. `Referrer-Policy: no-referrer` on `/responder/[token]` and `/invitacion/[token]`) belongs to Story 3.22, which actually wires up a client caller — this story ships no client-facing traffic yet, so there is nothing to set the header on.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-21-responder-invitation-route-handlers-client-fetch-integration.md`
  summary: `GET /api/responder/[token]` and `GET /api/invitacion/[token]`'s deliberate `500 internal_error` convention (instead of this codebase's usual `422 validation_error`) for a caught `Error` is untested — neither manager function throws for any input the test file's real-Supabase-only style can construct.
  evidence: Forcing a genuine RPC infra failure would require mocking inconsistent with this file's own established real-Postgres approach. Same class already deferred for Stories 3.15/3.17's non-realistic-path catch branches.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-21-responder-invitation-route-handlers-client-fetch-integration.md`
  summary: `POST /api/responder/[token]/submit`'s catch block maps every caught `Error` — including a genuine infra failure — to `422 validation_error`, potentially surfacing a raw infra error message to an anonymous, unauthenticated caller.
  evidence: Same already-logged systemic pattern across every mutating route in this codebase (Story 3.3's deferred-work entry), not unique to this domain in kind — though this domain's total lack of an auth gate does make the exposure marginally wider than elsewhere.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-22-responder-invitation-pages-thin-delegates.md`
  summary: `tests/integration/feedback-flag-toggle.test.ts`'s file-header comment ("`submitFeedbackResponse` has no flag... and is excluded here too") is now stale with respect to `USE_NEW_API_RESPONDER` — still accurate for the specific flag that file tests (`USE_NEW_API_FEEDBACK`), but could mislead a future reader about this action's overall flag status.
  evidence: Not touched by Story 3.22's diff; a one-line comment fix whenever that file is next edited (Story 3.17's own file, out of this story's own Never-boundary scope).

- source_spec: `_bmad-output/implementation-artifacts/spec-3-23-responder-invitation-feature-flag-rollback-safety.md`
  summary: `makeRpcResult`, the `next/navigation`/`next/cache` mocks, and the `getRedirectUrl` helper are now duplicated verbatim across 8 test files (`admin-members-auth-flag-toggle.test.ts`, `cycles-flag-toggle.test.ts`, `feedback-flag-toggle.test.ts`, `responder-invitation-flag-toggle.test.ts`, and the 4 `*-new-path-verification.test.ts` files).
  evidence: This initiative's established extract-on-reuse pattern keeps not triggering because no single story's own scope naturally forces the extraction. Worth a `tests/support/` shared module once a story actually needs to touch more than one of these files at once.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-24-responder-invitation-new-path-verification.md`
  summary: `tests/integration/responder-invitation-new-path-verification.test.ts`'s peer-invitee lifecycle fixture relies on implicit cross-`describe`-block execution order (Vitest's default in-file declaration order) with no `.sequential`/comment guarding against it.
  evidence: Matches the identical, universal, already-accepted pattern every characterization/new-path-verification file in this repo relies on (including Story 3.19's own frozen baseline) — not novel to this story, and Vitest's documented default behavior makes this a low-risk, low-priority hardening item.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-25-characterization-tests-read-only-reports-baseline.md`
  summary: Add multi-row ordering coverage for the "open company cycles" (`feedback_cycle_participants` -> `feedback_cycles`) and "cycle-request dedup" (`feedback_requests`, `request_type = cycle`) dashboard queries — both are still asserted only against a single-row result.
  evidence: A real gap (found during this story's 3-lens review, Edge Case Hunter), but closing it needs a second `create_feedback_cycle` fixture (a 2nd open company cycle dashboardEmployee participates in, plus a 2nd organized cycle request) — materially more fixture complexity than a direct correction, and not required to lock in current single-row behavior as this story's own frozen I/O matrix scopes it. Worth closing whenever this domain's tests are next substantially touched, or in a future dedicated multi-cycle scenario story.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-feedback-screens-redesign.md`
  summary: **BUG (production data-integrity, not a coverage gap)** -- `create_feedback_cycle`'s per-cycle question-cloning loop (`supabase/migrations/0023_one_open_cycle_at_a_time.sql:85-94`) SELECTs/INSERTs only `position, prompt, question_type, required, competency_code, applies_to` from `default_360_cycle`, never `self_only`/`saboteador_code` (added later by migration `0061_saboteadores.sql`). Every company/org 360 cycle created since 0061 landed clones its 10 saboteador questions with `self_only` defaulting to `false` and `saboteador_code` to `null`: they leak into peer evaluators' forms (questions meant only for the report subject's own self-reflection are shown to colleagues instead), and `get_request_saboteadores()` always returns zero rows for that cycle, silently disabling the entire "Tus saboteadores" report feature for every company/org account.
  evidence: Confirmed independently by direct migration read (the SELECT/INSERT column lists genuinely omit both fields) and empirically by the story's own implementer (zero `feedback_answers` anywhere in the local DB join to a `self_only=true`+`saboteador_code` question, across every prior company-360 fixture built this session). `create_individual_cycle_request` (migration `0057`, the individual-account "Pedir feedback 360" flow) is unaffected -- it reuses the base template directly rather than cloning, so `self_only`/`saboteador_code` survive intact there; this is why the story's own real-data fixture had to be built via the individual-account path specifically. Fix is a one-line column-list addition to the `INSERT`/`SELECT` in `0023_one_open_cycle_at_a_time.sql`'s cloning loop (a new migration, not editing the old one) -- out of this UI-only story's explicit "never touch migrations/RPCs" boundary. Recommend its own dedicated bug-fix story/spec, prioritized ahead of Epic 4 cosmetic work given it's a real, currently-live product defect (not merely a redesign gap) affecting every company account's saboteadores feature.

  **Second, more user-visible symptom of this same bug, independently observed during Story 4.4 (spec-4-4-responder-invitation-screens-redesign.md):** `get_responder_context`'s own exclusion filter (`0061_saboteadores.sql:141`, `and not (not is_self_flag and sq.self_only)`) is itself correct -- it only ever fails silently because `sq.self_only` is `false` for every company-cycle-cloned saboteador question (the exact bug above), making the exclusion a no-op. Practical effect confirmed live in this session's own testing: peer evaluators on a company 360 cycle are actually shown the 10 self-reflection saboteador questions in their own responder wizard -- not just an empty report on the subject's side, but visibly wrong, confusing questions on the evaluator's side too. This is the same root cause and the same fix (the `create_feedback_cycle` column-list correction above resolves both symptoms at once), not a second bug to fix separately.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-1-paginate-admin-organizations-list.md`
  summary: `list_organizations()`'s `left join members m on m.organization_id = o.id and m.is_supervisor` has no de-dup guard -- if any organization ever ends up with more than one `is_supervisor = true` member row, `total_count` and page slicing silently double-count it.
  evidence: No unique constraint on `is_supervisor` per organization exists anywhere in `supabase/migrations/`. Pre-existing: the same join, with the same latent shape, already existed unchanged in migration 0017 (supervisor_and_admin_management) since before pagination existed -- Story 6.1 only adds `total_count` on top of an already-existing join. A real fix is either a partial unique index (`create unique index on members (organization_id) where is_supervisor`) or a `distinct on (o.id)` in the RPC, and should be scoped as its own story since it touches supervisor-assignment invariants beyond this one screen.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-1b-delete-old-path-admin-reports.md`
  summary: `src/app/admin/page.tsx`, `src/app/admin/empresas/[id]/page.tsx`, and `src/app/dashboard/members/page.tsx` call Supabase directly with no rollback flag and no manager equivalent ever built -- they never got a "new path" to begin with, so this story's own old-path deletion (which collapsed `USE_NEW_API_ADMIN_MEMBERS`/`USE_NEW_API_REPORTS` in every file that had both branches) explicitly left them untouched, byte-for-byte.
  evidence: Investigated (not guessed) via repo-wide grep during this story: these 3 files hold direct `supabase.from(...)`/`supabase.rpc(...)` calls with no `process.env.USE_NEW_API_*` check anywhere in them, and no matching manager function exists for admin's own organization-listing/detail reads or the members-list page's own reads. Building that new manager-delegated path from scratch is out of scope for a legacy-path *deletion* story -- it's new construction, not a deletion, and should be scoped as its own story (likely as a follow-up "Become Thin Delegates" story for the admin-console/members-list reads, per the Epic 3 flag-clash pattern already on record).

- source_spec: `_bmad-output/implementation-artifacts/spec-5-1a-delete-old-path-cycles.md` (and spec-5-1b/spec-5-1c)
  summary: `docs/feature-flags.md` and `ARCHITECTURE-SPINE.md` AD-10 (the architectural decision record defining the per-domain rollback-safety-flag convention) are never updated to reflect that all 5 flags this story removes no longer exist.
  evidence: Both documents still describe a live, checkable convention (manual QA checklist, rollback procedure, "checked once per thin caller") for flags that no longer exist in the code after Stories 5.1a/5.1b/5.1c. Deferred rather than fixed here because it's a documentation-consistency pass spanning multiple planning artifacts, better done once Story 5.2 (import-boundary confirmation) and 5.3 (Supabase-removal readiness review) also land, so it's written once against the final state rather than incrementally per sub-story.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-1b-delete-old-path-admin-reports.md`
  summary: `src/app/dashboard/mi-mapa/page.tsx` and `src/app/dashboard/informe-empresa/page.tsx` fetch their two independent reads (framework data + map/summary data) sequentially, not in parallel via `Promise.all`, on the single path Story 5.1b kept.
  evidence: Traced via git diff against migration 0017/Story 3.28's own original two branches -- the *old* (now-deleted) path used `Promise.all` for these two reads; the *new* path Story 3.28 built already awaited them sequentially, and Story 5.1b's mechanical `if(flag){new}else{old}` collapse correctly kept the new path byte-for-byte, including this pre-existing characteristic. Not introduced by Story 5.1b -- pre-existing since Story 3.28 (Epic 3), whenever `USE_NEW_API_REPORTS=true` was active. Minor performance-only concern (one extra sequential round-trip per page load), not a correctness issue -- worth a trivial `Promise.all` fix as a follow-up, not urgent enough to justify touching these files again in this story.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-1-paginate-admin-organizations-list.md` (first surfaced), re-confirmed by spec-5-1a/5-1b/5-1c
  summary: `tests/integration/admin-organizations-pagination.test.ts`'s "default load" test intermittently fails when run as part of the full `npm test` suite (passes reliably in isolation) -- a real org-count race, not a flaky assertion.
  evidence: The test counts organizations via one RPC call, then renders `AdminPage` (a second, separate call) and asserts the two counts match. Other test files in the same `npm test` run create organizations concurrently against the same shared local Supabase instance, so a row can be inserted between the two calls. Reproduced directly: fails ~1 in 2 full-suite runs, passes 100% in isolation (`npx vitest run tests/integration/admin-organizations-pagination.test.ts`). Not caused by any single story -- a general property of this repo's shared-local-DB, no-test-isolation test design, that any two-call-with-an-assertion-between test in a list/count context will hit eventually. A real fix needs either per-file DB namespacing/transaction rollback (a real investment) or making this specific test derive its expected count from a single call rather than two -- worth a small, dedicated story rather than a side-fix here.

- source_spec: Story 6.3/6.4 (session-resolution completion + domain migrations -- cycles/feedback/report-groups/members/self/reports/auth-signup -- landed as part of the "complete db -> managers -> pages layering repo-wide" initiative)
  summary: **NEW test-infrastructure discovery**, surfaced only because this initiative ran its own characterization suites (plus 4 parallel implementer agents' own suites) against the same shared local Supabase instance many times over one session, pushing the `organizations` table past 1000 rows for the first time. `list_organizations()`'s zero-arg ("genuinely unbounded") call path is silently truncated at exactly 1000 rows by PostgREST's own default response-row cap, independent of anything in the SQL function itself -- three Story 6.1 tests that assert the zero-arg call returns literally every row (`admin-members-auth-manager.test.ts`, `admin-members-auth.test.ts`, `admin-members-auth-new-path-verification.test.ts`) started failing once the local DB's real row count exceeded 1000, with mismatches like "expected 1241 to be 1000".
  evidence: Reproduced directly -- `npx supabase db reset` (replaying all 67 migrations, wiping accumulated test fixture rows back under 1000) made the full suite pass 498/498 again immediately, with zero source changes. Confirmed via each failing assertion's own error message, which always reported the *true* row count on the right and a flat `1000` on the left -- the signature of a silent API-level page-size cap, not an SQL bug. Pre-existing and outside every domain this initiative touched (admin/Story 6.1 only) -- not fixed here. A real fix is either passing an explicit `p_limit` large enough to exceed realistic org counts from these 3 tests' own call sites, or raising PostgREST's `db-max-rows`/`max-rows` config for local dev -- worth a small follow-up story once Story 6.1's own admin-page migration (deferred-work entry above, `dashboard/members/page.tsx`... now superseded, see next entry) is revisited. In the meantime, `npx supabase db reset` is a reliable workaround for local dev whenever these 3 tests start failing with a suspiciously round `1000`.

- source_spec: Story 6.3/6.4 (session-resolution completion + domain migrations), supersedes the `src/app/dashboard/members/page.tsx` portion of the spec-5-1b deferred-work entry above
  summary: The earlier deferred-work entry logged against spec-5-1b listed `admin/page.tsx`, `admin/empresas/[id]/page.tsx`, **and** `dashboard/members/page.tsx` as three pages with no manager path ever built. This initiative fully migrated `dashboard/members/page.tsx` (new `membersManager.listMyOrganizationMembers()`/`listDepartments()`/`countActiveOrganizationMembers()`) along with the cycles, feedback, report-groups, and members/self/reports/auth-signup domains -- it is no longer part of that gap. Only the two platform-admin pages (`admin/page.tsx`, `admin/empresas/[id]/page.tsx`) remain unmigrated, `eslint.config.mjs`'s exemption list narrowed to just those two.
  evidence: `git diff` of this initiative's own commits; `grep -n "@/lib/supabase\|@supabase/supabase-js\|@/server/db" src/app/dashboard/members/page.tsx` returns nothing post-migration. The two remaining admin pages are platform-admin/cross-org scope, a deliberately separate follow-up (see the Epic 6 backlog entry "Complete manager delegation for remaining admin pages").

- source_spec: Story 6.3/6.4, the user-mandated 3-expert repo-wide MVC-compliance review that followed the domain migrations above
  summary: `reportGroupsManager`'s new `canClose` computation reproduces `close_report_group`'s pre-existing hardcoded UI display value of "5 accepted members required" bit-for-bit, but the RPC's own real enforcement at close time actually reads a per-organization `platform_settings.min_invitees_per_request` (falling back to a platform-wide default, itself floor-constrained to >= 5) -- so a page could theoretically show "closeable" using the constant while the RPC's own real per-org threshold differs. `get_report_group`'s RPC response carries no `organization_id`/threshold field to expose, so fixing this for real needs a migration/RPC change.
  evidence: Confirmed directly against `supabase/migrations/0064_report_groups.sql`'s `close_report_group` RPC body and `0001_initial_schema.sql`'s check constraint on `platform_settings.min_invitees_per_request`. Pre-existing (the page's own prior hardcoded `< 5` predates this initiative) -- not introduced or fixed by this initiative, just surfaced and documented in place (see the comment on `MIN_ACCEPTED_TO_CLOSE` in `src/server/db/reportGroups.ts`) while moving the existing behavior to the correct layer. Out of scope for a pure layering fix; worth its own small story alongside a `get_report_group` RPC change.

- source_spec: Story 6.3/6.4, the `dashboard/page.tsx` business-logic-leakage remediation specifically
  summary: Two small, disclosed trade-offs from moving `dashboard/page.tsx`'s inline computations into managers. (1) `cyclesManager.getMyCycleRequests()` now runs twice per dashboard render (once inside `getMyPendingRequestsSummary()`, once inside `getCyclesNeedingOrganization()`) where the page previously fetched it once and reused the result for both -- a minor, non-hot-path extra round-trip, traded for each manager function being an independently correct, self-contained unit. (2) `tests/integration/read-only-reports-thin-delegate.test.ts` lost 2 scenarios (a partial-failure-survives-dedup case and an interleaved multi-row sort-order case) that used to probe the page's own merge/sort internals directly -- those internals now live in `feedbackManager.getMyPendingRequestsSummary()`, and forcing one of its two composed reads to fail deterministically against a real Postgres instance for an equivalent characterization test wasn't practical to build in the same pass. The merge/sort code itself was moved verbatim (not reimplemented), so behavioral risk is low, but this specific edge is currently verified by code inspection rather than a dedicated test.
  evidence: Both self-reported by the implementing agent during the fix, cross-checked by re-reading the relevant diffs (`src/server/managers/cyclesManager.ts`'s `getCyclesNeedingOrganization`, `feedbackManager.ts`'s `getMyPendingRequestsSummary`, and the rewritten `read-only-reports-thin-delegate.test.ts`). Worth closing whenever this file's test suite is next substantially touched -- e.g. a small real-Postgres characterization test that seeds one ad-hoc and one cycle request with distinct timestamps and asserts interleaved sort order, plus a mock-level test that forces just the ad-hoc half to reject and asserts the cycle half still renders.

- source_spec: Story 7.1 (schema port) / Story 7.6 follow-up (individual-account evaluator update), `src/app/dashboard/feedback/[id]/page.tsx`
  summary: Both `update_ad_hoc_feedback_request_evaluators` (member ids) and `update_ad_hoc_feedback_request_evaluators_for_individual` (emails) are add-only at the RPC level as of their final redefinition (renumbered `0094_fix_ambiguous_returning_column.sql`) -- `insert ... where not exists (...)`, never a `delete` -- regardless of response count. Both UI branches on this page (`EvaluatorPicker` for company accounts, `EmailEvaluatorPicker` for individual accounts) let the user uncheck/remove an already-invited entry client-side before submitting, implying removal works; it doesn't -- the form submits the full desired list, but the RPC silently keeps every previously-invited entry regardless, so an unchecked-then-submitted removal is quietly discarded server-side.
  evidence: Confirmed directly by reading both RPCs' final bodies in `supabase/migrations/0094_fix_ambiguous_returning_column.sql` (no `delete from feedback_invitations` in either). Pre-existing since Story 7.1 ported the schema (the member-id branch already existed in `brujula-core` before Story 7.6's follow-up added the individual-account branch alongside it, inheriting the same characteristic) -- not introduced by either story, just newly surfaced while adding the individual-account branch. A real fix is either (a) making both RPCs actually delete un-listed invitees again (reverting the upstream add-only change, likely undesirable since it was a deliberate upstream fix for a different problem -- re-sending invitation emails to already-invited people), or (b) removing the "uncheck to remove" affordance from both `EvaluatorPicker`/`EmailEvaluatorPicker` UIs to match the RPCs' real add-only behavior, or (c) building a genuine remove path (a new, explicit "remove evaluator" RPC/action) if removal is still a real product need. Worth its own small story; out of scope for a schema-port/follow-up fix.

- source_spec: Story 7.3 (Biblioteca/thresholds/AI interpretation), `src/components/CompetencyRadar.tsx` -- FIXED, not deferred
  summary: `CompetencyRadar.tsx`'s `GROUP_LABELS` constant displayed "Visionario" for the `visionario` role code -- stale since the competency-model-v2 migration (renumbered `0072_competency_model_v2.sql`) renamed the role's display name to "Visión" (deliberately keeping the DB `code` unchanged so existing frontend indexing by code wouldn't break). Found while building Story 7.3's `CompetencyModelDiagram.tsx` (which reads the real DB name instead and correctly showed "Visión", exposing every OTHER page -- `mi-mapa/page.tsx`, report/comparison charts -- as still stale via this shared constant).
  evidence: One-line fix applied directly after Story 7.3 landed (low-risk, already-shipped user-visible pages) rather than left deferred: `visionario: "Visionario"` -> `"Visión"`. The other 4 labels (`arquitecto`/`catalizador`/`coach`/`plenitud`) were confirmed unchanged by the same migration, so no other stale entries exist in this constant.

- source_spec: Story 7.7 (evaluator-email-only-new + ResponderWizard validation), `src/app/actions/auth.ts`
  summary: `acceptInviteSignUp`/`individualSignUp` still hardcode their own signup-success copy ("te hemos enviado un correo de confirmación...") instead of reading it from `platform_texts`, even though migration `0095_signup_success_texts.sql` (renumbered, landed in Story 7.1) already seeded the `signup_success_invite`/`signup_success_individual` keys for exactly this purpose -- the same pattern already used everywhere else editable copy in this app lives (`platform_texts` + `feedbackManager.getPlatformText()`).
  evidence: Found directly while porting Story 7.7's own invitation-email copy work (which did the equivalent alignment for `responderManager.ts`'s fallback string). `actions/auth.ts` was outside that story's declared file scope. A small, low-risk follow-up: swap the two hardcoded strings for `feedbackManager.getPlatformText("signup_success_invite"/"signup_success_individual", <same current string as fallback>)`, mirroring every other `platform_texts`-driven copy call site already in the codebase.

- source_spec: Story 7.5 (Onboarding360Wizard), `src/app/actions/cycles.ts`
  summary: `cycles/nueva/page.tsx` and `feedback/nueva-360/page.tsx` now submit through page-local, inline `"use server"` actions (`startCycle`/`submitIndividualRequest`) shaped for `useActionState` (return state instead of redirecting, so the wizard can render its own confirmación step) rather than through the shared `src/app/actions/cycles.ts` exports (`createFeedbackCycle`/`createIndividualCycleRequest`), which still redirect and are no longer called by either page. Both page-local actions call the exact same manager functions (`cyclesManager.createCycle`/`createIndividualRequest`) with the same parsing/error-handling shape, so there's no behavior-duplication risk today, but it is a second copy of what's functionally the same Server Action logic -- root cause is the shared exports still using the older redirect-based shape, incompatible with `useActionState`'s "return a value the component can render" contract the wizard needs.
  evidence: Documented directly in Story 7.5's own implementation report; `actions/cycles.ts` was outside that story's declared file scope. A real fix is migrating `createFeedbackCycle`/`createIndividualCycleRequest` themselves to the `useActionState` shape and deleting the two page-local duplicates -- worth doing before a third page needs the same wizard-style flow, since a 3rd inline copy would be the point duplication stops being cheap.

- source_spec: Story 7.5 (Onboarding360Wizard), two minor staleness items surfaced by its repo-wide sweep for hardcoded question-count/order assumptions
  summary: (1) `src/lib/competencyAxes.ts`'s own comment still says "15 competencias" -- stale since the model-v2 migration brought the real count to 16 (renumbered `0072_competency_model_v2.sql`); comment-only, no logic depends on the literal number. (2) The `individual_360_intro` row in `platform_texts` (seeded by migration `0044`) is now orphaned/unreferenced, since `feedback/nueva-360/page.tsx` switched to the `onboarding_360_*` keys (migration `0078`, Story 7.5) for its intro copy.
  evidence: Both found directly during Story 7.5's own sweep, neither fixed (pre-existing, outside that story's scope -- a stale comment and an unused DB row, both harmless as-is). Low-priority cleanup: fix the comment's number, and either delete the orphaned `platform_texts` row or repurpose it if a future story needs a distinct individual-account intro variant.
